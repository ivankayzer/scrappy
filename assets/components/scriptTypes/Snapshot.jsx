import React from "react";
import ScriptDescription from "../ScriptDescription";

const Snapshot = () => (
  <div>
    <ScriptDescription description="Create a snapshot of the page and compare it between runs" />
  </div>
);

export default Snapshot;
